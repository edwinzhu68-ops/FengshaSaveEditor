# 烽沙存档修改器 · 功能说明

## 已实现

| 功能 | 说明 |
| --- | --- |
| 存档选择 | 默认读取 `%LOCALAPPDATA%\\MOProject\\Saved\\SaveGames`，支持选择整个存档目录或单个槽位文件夹 |
| 单位属性 | 修改已识别单位的移速、生命、攻击、护甲、士气、质量、模型比例等字段 |
| 批量单位 | 可修改全部匹配单位，也可以只修改一个匹配单位区域 |
| 民夫移速 | 一次修改当前存档中全部已识别民夫的移速 |
| 自有兵种移速 | 一次修改已识别的民夫、常规兵种和攻城器械类型，不包含野兽、建筑和城防设施 |
| 玩家属性 | 修改搬运容量、搬运效率、采集效率、制作效率、政策移速等已确认的玩家全局字段 |
| 资源规模 | 按资源类别和小型/中型/大型规模修改全部匹配资源点 |
| 资源批量 | 可将全部已识别资源点统一设置为 99,999 |
| 备份恢复 | 每次写入前自动整槽备份，支持列出、校验和恢复备份 |
| 完整校验 | 检查 VSOM CRC、Oodle 分块、GVAS 长度/魔数，并对写回文件完整解压回读 |
| 捐赠页 | 展示作者提供的微信/支付宝二维码和 QQ：35611294；不接入支付、不参与功能解锁 |

## 安全边界

* 工具只修改所选槽位的 `Mass.sav`、`Level.sav`、`Player.sav` 和必要的 `Slot.sav`，不会修改游戏安装目录。
* 写入前自动备份，候选文件验证失败时不会替换原文件；写入后还会再次回读校验。
* 游戏运行中可以写入，但如果游戏同时保存，游戏保存可能覆盖工具刚写入的结果。
* 道路速度、单位模板、BuffConfig 等配置层字段目前仍是只读研究范围，不会猜测写入。
* 资源设置为 99,999 是一次性存档修改，不是永久内存锁定；采集后仍可能减少。

## 构建

需要 Windows x64、.NET 8 SDK 和用户自己游戏目录中的 `oo2core_9_win64.dll`。代码仓库不分发游戏 DLL、真实存档或个人收款二维码。

```powershell
dotnet build FengshaSaveEditor.csproj
dotnet run --project FengshaSaveEditor.csproj -- --help
```

若要生成完整发行包，需把 Oodle DLL 放入发行目录，并按作者自己的发行资源加入捐赠二维码和图标。

## 使用原则

请先复制存档再尝试新数值。工具针对当前已验证的《烽沙》存档结构工作，游戏更新后应先使用 `--verify` 检查，不要直接覆盖唯一存档。
