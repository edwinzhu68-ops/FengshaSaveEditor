# 烽沙 · 存档工坊 UI 设计整合说明

## 最终采用的方向

两份评审意见合并后，采用“可靠的存档工作台”定位，不做成黑客面板或页游式外挂界面。

* 当前采用白底黑字的极简工具界面，使用浅灰边框区分编辑区域。
* 成功用低饱和绿色，警告用琥珀色，错误用低饱和朱红；颜色只辅助文字状态，不承担唯一信息。
* 正文使用 Microsoft YaHei UI；数值、CRC 和内部字段使用 Consolas。
* 不使用火焰动画、血迹、龙纹、仿古木纹和大面积鲜红。
* 所有状态同时显示文字和颜色，不能只依赖颜色；不额外堆叠图标或说明卡片。

## 窗口结构：暗黑 2 式存档编辑逻辑

为了让非技术用户一眼就会用，界面不采用多级侧栏和卡片堆叠，而采用成熟存档编辑器常见的四段结构：

1. 顶部工具栏：打开存档、重新读取、保存修改。
2. 存档栏：默认读取 SaveGames 文件夹；没有槽位时再选择整个存档目录或某个槽位文件夹，并显示核心文件数量和游戏是否正在运行。
3. 标签页编辑区：单位属性、资源、玩家属性、高级功能、捐赠；前四个标签只放可执行的修改控件，捐赠页只放二维码和联系方式。
4. 每个编辑页内部固定为“左边对象列表 + 右边编辑面板”：先搜索并选中民夫/资源规模/玩家属性，再在右边输入值、预览并保存。资源左侧每一行代表一种资源和一种规模。

底部只保留一条简洁状态栏。详细命令日志不在界面展示，状态栏只显示当前操作结果；窗口缩小时优先压缩左侧列表，编辑面板保持固定输入宽度。游戏运行中也允许保存，但同时保存可能产生覆盖冲突。

## 存档文件夹文案

软件必须明确告诉用户：

“一个存档由多个 .sav 文件组成。请选择 SaveGames 文件夹，或直接选择包含 Mass.sav 和 Level.sav 的存档槽文件夹，不要只选择某一个 .sav 文件。”

四个文件的职责：

* Mass.sav：单位、民夫和当前实体属性。
* Level.sav：资源点、地图和部分建筑数据。
* Player.sav：玩家全局 AT_ 属性。
* Slot.sav：槽位元数据。

## 功能分层

### 显示与输入规则

* 所有输入框在读取槽位后回填存档当前值；没有读到时显示“尚未读取”，不使用几千、几万的假默认值。
* 倍率字段统一显示为“1 倍、1.5 倍、2 倍”；程序保存时再换算为存档原始整数。
* 高级移速名称使用“所有民夫移速”和“全部自有兵种移速”；后者按已识别的民夫、常规兵种和攻城器械类型处理，不包含野兽、建筑和城防设施。
* “0×3”改为“0（3 项）”；资源点数量显示为“3 处”，不使用内部技术术语。
* 资源左侧显示资源名称、规模和资源点数量；规模按同类资源容量排序为小型/中型/大型，只有一个规模时显示“单一规模”。
* 资源页提供“全部资源统一为 99,999”；它会一次修改所有已识别资源类别和所有规模，采集后仍可能减少，因此不称为永久锁定。

### 免费

* 选择目录、识别槽位、只读扫描。
* CRC、Oodle、GVAS 和写回后的完整回读校验。
* 资源点按类别和规模批量修改。
* 全部资源统一为 99,999；这是一次性存档修改，不承诺采集后永久不减少。
* 备份列表、备份校验和恢复。
* 配置研究只读信息。
* 高级功能页中的单位、玩家属性修改。

### 捐赠

* 展示微信支付二维码、支付宝二维码和联系 QQ：35611294。
* 捐赠完全自愿，不参与功能解锁，不读取支付结果。

### 高级功能

* 民夫、兵种和攻城器械等已识别单位属性。
* 单位移速、生命、攻击、护甲、士气、质量、模型比例等已确认字段。
* 玩家搬运、采集、制作、政策移速和已确认的玩家 AT_ 字段。
* 全部匹配单位区域或单个匹配单位区域范围。

以上功能与资源、备份、恢复和校验一样免费开放。道路速度、单位模板和 BuffConfig 等仍未完成安全写回验证，不放入当前可修改列表。

## 当前验证边界

当前图形界面已经接入已验证的存档层命令，并新增动态单位列表、动态玩家 AT_ 列表、单实例范围入口和资源批量入口。单位/玩家扫描会区分标准 GVAS `IntProperty` 与本游戏已确认的 `FString` 键值布局；任一布局都无法确认时不提供写回。

道路、单位模板和 BuffConfig 仍未完成安全写回验证，因此不放入当前修改页，也不把“找到字段”写成“游戏效果已验证”。

## 捐赠实现边界

捐赠页只展示作者提供的静态二维码和联系方式，不接入微信/支付宝自动下单、回调、订单查询或授权系统。二维码未提供前显示“二维码待添加”，不会伪造支付成功状态。具体说明见 `捐赠说明.md`。

## 成熟工具参考

界面不照搬其他软件的素材，而是吸收成熟工具已经验证过的交互结构：

* GORE Save Editor 把存档编辑与游戏安装包 modding 分开，并强调只编辑存档、每次修改前自动备份。
* Rusted PackFile Manager 使用树状对象导航、可筛选的模式化表格、全局搜索和诊断区。
* Save Wizard 采用“先识别游戏/存档，再选择项目，最后统一 Apply”的明确流程。

因此本工具当前版采用“打开一次、标签切换、左选右改、最后保存”的工作流：单位页左侧列出已识别单位，资源页左侧列出类别，玩家页左侧列出已发现字段；每页都把范围、原值→新值和应用按钮放在同一编辑面板内。它仍然保留本游戏的特殊边界：存档选择按文件夹处理，配置层默认只读研究。
